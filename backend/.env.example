# ============================
# Backend Environment Variables - EXAMPLE
# Phase-2 Web Todo Application
# DO NOT COMMIT THIS FILE TO VERSION CONTROL
# ============================

# ---- App Environment ----
BACKEND_ENV=development
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# ---- CORS Configuration ----
# Comma-separated list of allowed origins for CORS
# For development: localhost URLs
# For production: your frontend domain URLs or leave empty to allow all origins
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ---- Authentication ----
AUTH_SECRET=your-secret-jwt-key-here
AUTH_TOKEN_EXPIRE_MINUTES=60

# ---- Database Configuration ----
# For Neon PostgreSQL - USE ENVIRONMENT VARIABLES for sensitive data
NEON_DB_USER=your_neon_username
NEON_DB_PASS=your_neon_password
DATABASE_URL=postgresql+asyncpg://${NEON_DB_USER}:${NEON_DB_PASS}@ep-your-project.your-region.aws.neon.tech/your_database_name

# For local development (SQLite)
# DATABASE_URL=sqlite:///./todo_app_local.db

# For testing
TEST_DATABASE_URL=postgresql://${NEON_DB_USER}:${NEON_DB_PASS}@ep-your-project.your-region.aws.neon.tech/your_database_name

# ---- JWT / Auth (FastAPI) ----
JWT_SECRET_KEY=your-super-secret-jwt-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ---- Connection Pooling (Production Values) ----
POOL_SIZE=20
MAX_OVERFLOW=0
POOL_TIMEOUT=30
POOL_RECYCLE=300
POOL_PRE_PING=True

# ---- Logging Configuration ----
LOG_LEVEL=INFO

# ---- Phase III: AI Chat Configuration ----
# OpenAI API Key for Agents SDK
OPENAI_API_KEY=your-openai-api-key-here

# Chat Configuration
CHAT_MAX_MESSAGES=50
CHAT_MAX_MESSAGE_LENGTH=10000